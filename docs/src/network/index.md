# 计算机网络

## 一、简介


计算机网络通过通信链路和交换设备，将地理上分散的自主计算机互连，以实现资源共享和信息传输。核心是分层协议体系（如 TCP/IP 模型），每一层为上层提供服务，同时对下层屏蔽实现细节。数据被封装成分组（包），通过存储转发和路由选择机制，在由路由器等设备构成的交换网络中，逐跳从源端到达目的端。传输层协议如 TCP 提供可靠、有序、面向连接的字节流，UDP则提供无连接、尽力而为的数据报传输。

## 二、目录